{
  "name": "sport360-scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Sport360 department scheduler with profile claiming, rotations, vacation approvals, and Supabase schema.",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js",
    "backup:manual": "node scripts/freePlanBackup.js",
    "check": "node --check server.js && node --check scripts/runtimeConfig.js && node --check scripts/smokeServer.js && node --check scripts/freePlanBackup.js && node --check src/config.js && node --check src/data.js && node --check src/supabaseStore.js && node --check src/scheduleLogic.mjs && node --check src/main.js",
    "test": "node --test test/scheduleLogic.test.mjs",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
